<!doctype html>
<html lang="fa">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Major Not Found</title>
  <style>
    :root {
      --bg: #f6f2ea;
      --ink: #1d1b16;
      --muted: #6b6358;
      --card: #fff8ee;
      --accent: #ff8f2f;
      --accent-2: #2f6cff;
      --shadow: rgba(20, 18, 14, 0.12);
      --line: rgba(29, 27, 22, 0.08);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: "Plus Jakarta Sans", "Segoe UI", system-ui, sans-serif;
      color: var(--ink);
      background:
        radial-gradient(1200px 600px at 80% -10%, #ffe0c2 0%, transparent 60%),
        radial-gradient(900px 500px at 10% 10%, #d7e4ff 0%, transparent 55%),
        var(--bg);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 32px 20px;
    }

    .wrap {
      width: min(820px, 100%);
      background: var(--card);
      border-radius: 24px;
      padding: 40px 36px;
      box-shadow: 0 20px 60px var(--shadow);
      border: 1px solid var(--line);
    }

    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 24px;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      font-weight: 700;
      letter-spacing: 0.2px;
    }

    .brand-badge {
      width: 40px;
      height: 40px;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--accent), #ffb457);
      display: grid;
      place-items: center;
      color: #fff;
      font-weight: 700;
    }

    h1 {
      font-size: clamp(26px, 3.6vw, 36px);
      margin: 0 0 10px;
    }

    p {
      margin: 0;
      color: var(--muted);
      line-height: 1.7;
      font-size: 16px;
    }

    .panel {
      background: #fff;
      border-radius: 16px;
      padding: 20px;
      border: 1px solid var(--line);
      display: grid;
      gap: 10px;
      margin: 20px 0 24px;
    }

    .major {
      font-weight: 700;
      color: var(--ink);
      word-break: break-word;
    }

    .actions {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    a.button {
      text-decoration: none;
      color: #fff;
      background: linear-gradient(135deg, var(--accent-2), #6aa0ff);
      padding: 12px 20px;
      border-radius: 12px;
      font-size: 15px;
      box-shadow: 0 12px 24px rgba(47, 108, 255, 0.25);
      display: inline-block;
    }
  </style>
</head>
<body>
  <main class="wrap">
    <header>
      <div class="brand">
        <div class="brand-badge">BG</div>
        <div>BranchGuide</div>
      </div>
      <div>Majors</div>
    </header>

    <h1>Major Not Found</h1>
    <div class="panel">
      <p>The major you requested could not be found:</p>
      <p class="major"><%= title %></p>
    </div>

    <div class="actions">
      <a class="button" href="/">Back to Home</a>
    </div>
  </main>
</body>
</html>
